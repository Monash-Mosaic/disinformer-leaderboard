import { redirect } from "next/navigation";

export default function Home() {

  return (
    <div className="flex min-h-screen items-center justify-center bg-[#ffffef] font-sans dark:bg-black">
      <h1 className="text-4xl font-bold">Home</h1>
    </div>
  );
}
